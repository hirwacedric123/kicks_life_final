# Core Django
Django==5.2
asgiref==3.9.1
sqlparse==0.5.3

# Database
psycopg2-binary==2.9.10
dj-database-url==2.2.0

# Production server
gunicorn==23.0.0
whitenoise==6.8.2

# REST API
djangorestframework==3.16.0
djangorestframework_simplejwt==5.5.0
django-filter==24.2

# CORS
django-cors-headers==4.7.0

# Image processing (for product images)
Pillow==11.2.1

# PDF generation (for reports)
reportlab==4.4.1

# JWT (for API authentication)
PyJWT==2.9.0

# Environment variables
python-dotenv==1.1.0
python-decouple==3.8

# Utilities
pytz==2025.2
tzdata==2025.2

# HTTP requests (if needed for external APIs)
requests==2.32.3

# Optional: Admin theme (remove if not using)
django-jazzmin==3.0.1

# Optional: Widget tweaks (remove if not using)
django-widget-tweaks==1.5.0

# Optional: Auto logout (remove if not using)
django-auto-logout==0.5.1
